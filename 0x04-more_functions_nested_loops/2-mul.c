#include "main.h"

/**
  * mul - return multiply of two ints
  * @i : input as int
  * @s : input as int
  *
  * Return: function will return multiply of two ints
  */

int mul(int i, int s)
{
	return (i * s);
}
