#include "main.h"

/**
  * is_palindrome - will check if int is prime
  * @s : input char
  *
  * Return: return int
  */

int is_palindrome(char *s)
{
	if (s[0] == s[1])
		return (1);
	return (0);
}
